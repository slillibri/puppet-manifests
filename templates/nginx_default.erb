server{
  server_name <%= nginxservername || 'localhost' %>;
  listen <%= nginxport || '80' %>;
  
  location / {
    root <%= nginxroot || '/var/www' %>;
    index <%= nginxindex || 'index.html' %>;
  }
}