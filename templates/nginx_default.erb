<% $nginxservername |= 'localhost'%>
<% $nginxport |= '80' %>
<% $nginxroot |= '/var/www' %>
<% $nginxindex |= 'index.html' %>

server{
  server_name <%= $nginxservername %>;
  listen <%= $nginxport %>;
  
  location / {
    root <%= $nginxroot %>;
    index <%= $nginxindex %>;
  }
}